import type { Config } from 'tailwindcss'

// Tailwind v4: most config moved to globals.css @theme directive
// This file kept for any v4-incompatible plugins or legacy interop
const config: Config = {
  darkMode: 'class',
}

export default config
